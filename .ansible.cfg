[defaults]

retry_files_enabled = False
host_key_checking = False
remote_tmp = /var/tmp/$USER/.ansible/tmp

inventory = ~/harden/inventory.yml

roles_path = ~/harden/roles

[privilege_escalation]
become=True
become_method=sudo
become_user=root
