[defaults]

retry_files_enabled = False
host_key_checking = False
remote_tmp = /var/tmp/$USER/.ansible/tmp

inventory = ~/repos/misc/harden_role/inventory.yml

roles_path = ~/repos/misc/harden_role/roles

[privilege_escalation]
become=True
become_method=sudo
become_user=root
